{% extends 'base.html.twig' %}

{% block title %}Debts Stats{% endblock %}

{% block body %}
{#}
    <div style="width: 80%; margin: auto;">
        <canvas id="debtChart"></canvas>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // Get the data for the chart from the data attribute in the template
            const data = {{ debtChartData|raw }};

            // Create the chart
            const ctx = document.getElementById('debtChart').getContext('2d');
            const myChart = new Chart(ctx, {
                type: 'bar',
                data: data,
                options: {
                    scales: {
                        y: {
                            beginAtZero: true
                        }
                    }
                }
            });
        });
    </script>
{#}
{# debt/stat.html.twig #}

    <h1>Debt Statistics</h1>
    <div id="bar_chart_div" style="width: 100%; height: 500px;"></div>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
        {{ gc_draw(barChart, 'bar_chart_div') }}
    </script>

    <div class="card-footer">
            <a href="{{ path('app_debt_index') }}" class="btn btn-secondary">Back to List</a>
        </div>
{% endblock %}
